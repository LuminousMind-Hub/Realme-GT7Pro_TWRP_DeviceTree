# ADB
ro.adb.secure=0

# Gatekeeper
vendor.gatekeeper.is_security_level_spu=0

# Battery Property
ro.charger.enable_suspend=1

persist.sys.fuse.passthrough.enable=true

# USB Config
vendor.usb.use_ffs_mtp=1

# seliunx 
ro.seliunx=permissive